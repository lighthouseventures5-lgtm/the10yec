---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import triadImage from '../assets/images/triad-of-three-pillars.png';
---

<BaseLayout title="The10 Youth Excellence Center - Yoga, Technology & Entrepreneurship">
  <!-- Hero Section -->
  <section class="hero">
    <div class="container hero-content">
      <div class="hero-text">
        <div class="hero-label">GRATITUDE. YOUTH EXCELLENCE.</div>
        <h1>
          Where <span class="text-gold">Ancient Wisdom</span><br>
          Meets <span class="text-blue">Modern Innovation</span>
        </h1>
        <p class="hero-subtitle">
          <strong>Youth Entrepreneurship and Excellence with Yogic Powers</strong><br>
          Samyama, Yama and Niyama
        </p>
        <p class="hero-description">
          Empowering the next generation through the integration of yoga, technology, 
          entrepreneurship, and community service. Build clarity, confidence, resilience, 
          and purpose while creating positive impact in society.
        </p>
        <div class="hero-buttons">
          <a href="/#programs" class="btn btn-primary">Explore Programs</a>
          <a href="/about/philosophy" class="btn btn-outline">Our Philosophy</a>
        </div>
        <div class="hero-stats">
          <div class="stat">
            <div class="stat-number">4</div>
            <div class="stat-label">Core Pillars</div>
          </div>
          <div class="stat">
            <div class="stat-number">100+</div>
            <div class="stat-label">Youth Empowered</div>
          </div>
          <div class="stat">
            <div class="stat-number">2</div>
            <div class="stat-label">Locations</div>
          </div>
        </div>
      </div>


    </div>
  </section>

  <!-- Why The10 Exists -->
  <section id="about" class="section reveal">
    <div class="container text-center">
      <div class="section-header">
        <span class="section-label">OUR MISSION</span>
        <h2>Why The <span class="text-gradient-gold">10YEC</span> Exists</h2>
        <p>We are addressing three critical challenges facing today's youth through ancient wisdom and modern application.</p>
      </div>

      <div class="challenges-grid">
        <div class="challenge-card shadow-premium-hover reveal-stagger">
          <div class="challenge-icon">🧠</div>
          <h3>1. Mind Control</h3>
          <h4>Harness the Power of Mind (Samyama)</h4>
          <p>We restore the 'human' superpower with Samyama. Achieve Total Mind Control for excellence in every endeavor.</p>
        </div>

        <div class="challenge-card shadow-premium-hover reveal-stagger">
          <div class="challenge-icon">🌍</div>
          <h3>2. Social Consciousness</h3>
          <h4>Conscious entrepreneurship (Yama and Niyama)</h4>
          <p>In a world of growing inequality & divisiveness, we need socially conscious 'youth entrepreneurs' grounded in Yama & Niyama.</p>
        </div>

        <div class="challenge-card shadow-premium-hover reveal-stagger">
          <div class="challenge-icon">🔗</div>
          <h3>3. Connected Learning</h3>
          <h4>Unify science, spirituality, entrepreneurship, faith and holistic wellness</h4>
          <p>We offer canonical & freedom learning to connect the dots. There are infinite ways to get to Infinity!</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Triad of 3 Pillars -->
  <section class="section programs-section triad-section reveal">
    <div class="container">
      <div class="section-header text-center">
        <span class="section-label">THE 10YEC SOLUTION</span>
        <h2>Triad of <span class="text-gradient-gold">3 Pillars</span></h2>
        <p>Three interconnected pathways to personal growth, community impact, and purposeful innovation</p>
      </div>

      <div class="triad-visual" style="margin-bottom: 4rem;">
         <Image src={triadImage} alt="Triad of 3 Pillars: Yoga, Technology, Sharing" class="triad-img hover-scale" />
      </div>
    </div>
  </section>

  <!-- Programs Section -->
  <section id="programs" class="section programs-cards-section reveal">
    <div class="container">
      <div class="section-header text-center">
        <span class="section-label">OUR PROGRAMS</span>
        <h2>Explore Our <span class="text-gold">Programs</span></h2>
        <p>Discover the four pillars of excellence that guide our holistic approach to youth development</p>
      </div>

      <div class="programs-grid">
        <div class="program-card shadow-premium-hover reveal-stagger">
          <div class="program-icon">🧘</div>
          <h3>Yoga & Wellness</h3>
          <p>
            Establishing a strong physical and mental foundation through Yoga Foundation Camps & Classes. 
            Express yourself through Yoga Dance Flows and compete at the highest level.
          </p>
          <ul class="program-features">
            <li>Yoga Foundation Camps & Classes</li>
            <li>Yoga Dance Flows</li>
            <li>Championship Coaching</li>
          </ul>
          <a href="/programs/yoga" class="program-link">Learn More →</a>
        </div>

        <div class="program-card shadow-premium-hover reveal-stagger">
          <div class="program-icon">💡</div>
          <h3>Entrepreneurship</h3>
          <p>
            Building the next generation of innovators through comprehensive Foundation & Leadership Camps, 
            Public Speaking, and proven methodologies.
          </p>
          <ul class="program-features">
            <li>Leadership Camps</li>
            <li>Public Speaking</li>
            <li>I-O-A-M Framework</li>
          </ul>
          <a href="/programs/entrepreneurship" class="program-link">Learn More →</a>
        </div>

        <div class="program-card shadow-premium-hover reveal-stagger">
          <div class="program-icon">🤖</div>
          <h3>Technology</h3>
          <p>
            Preparing youth for the future with hands-on experiences in cutting-edge technologies, 
            robotics, AI, and startup acceleration.
          </p>
          <ul class="program-features">
            <li>Robotics</li>
            <li>AI Hackathons</li>
            <li>Startup Accelerator</li>
          </ul>
          <a href="/programs/technology" class="program-link">Learn More →</a>
        </div>

        <div class="program-card shadow-premium-hover reveal-stagger">
          <div class="program-icon">🌟</div>
          <h3>Sharing & Service</h3>
          <p>
            Creating meaningful impact through Humanitarian Projects, community leadership, 
            and structured frameworks for positive change.
          </p>
          <ul class="program-features">
            <li>Humanitarian Projects</li>
            <li>Community Leadership</li>
            <li>SEWA Framework</li>
          </ul>
          <a href="/programs/sharing" class="program-link">Learn More →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Philosophy Section -->
  <section id="resources" class="section philosophy-section">
    <div class="container-narrow text-center">
      <div class="section-header">
        <span class="section-label">OUR PHILOSOPHY</span>
        <h2>Designing Excellence in You</h2>
      </div>

      <div class="philosophy-quote">
        <svg class="quote-mark" width="60" height="60" viewBox="0 0 60 60">
          <path d="M20 30c0-5.523 4.477-10 10-10V10C18.954 10 10 18.954 10 30s8.954 20 20 20v-10c-5.523 0-10-4.477-10-10z" fill="currentColor" opacity="0.2"/>
        </svg>
        <p>"We have to become zero to become 'The One'"</p>
        <span class="philosophy-author">— THE10 PHILOSOPHY</span>
      </div>

      <div class="growth-framework">
        <h3>Our Philosophy of Growth</h3>
        <p>At The 10 Youth Excellence Center, we believe in a holistic approach to growth—rooted in deep inner mastery and mindful action.</p>
        
        <div class="framework-steps">
          <div class="framework-step">
            <div class="step-icon">💡</div>
            <h4>Insight</h4>
            <p>Seeing clearly—understanding oneself, the world, and the systems we live in.</p>
          </div>
          <div class="arrow">→</div>
          <div class="framework-step">
            <div class="step-icon">🧘</div>
            <h4>Action</h4>
            <p>Translating awareness into disciplined, ethical, and purposeful action.</p>
          </div>
          <div class="arrow">→</div>
          <div class="framework-step">
            <div class="step-icon">🎯</div>
            <h4>Outcomes & Impact</h4>
            <p>Creating measurable change in personal life, profession, community, and society.</p>
          </div>
        </div>
      </div>
    </div>
  </section>



  <!-- Centers Section -->
  <section class="section centers-section">
    <div class="container">
      <div class="section-header text-center">
        <span class="section-label">OUR LOCATIONS</span>
        <h2>The 10YEC Centers</h2>
      </div>

      <div class="centers-grid">
        <div class="center-card">
          <div class="center-image">
            <div class="image-placeholder">
              <span>🏘️</span>
              <p>The 10YEC Ranch</p>
            </div>
          </div>
          <div class="center-content">
            <h3>Nature Retreat & Training Center</h3>
            <p>Youth Entrepreneurship Launch</p>
            <div class="center-location">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
                <circle cx="12" cy="10" r="3"/>
              </svg>
              <span>7600 Cottonwood St, Frisco, TX</span>
            </div>
          </div>
        </div>

        <div class="center-card">
          <div class="center-image">
            <div class="image-placeholder">
              <span>🌏</span>
              <p>India Campus</p>
            </div>
          </div>
          <div class="center-content">
            <h3>Youth Excellence Center</h3>
            <p>Programs across India</p>
            <div class="center-location">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"/>
                <circle cx="12" cy="10" r="3"/>
              </svg>
              <span>Multiple Locations in India</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* Hero Section */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 8rem 0 4rem;
    background: var(--gradient-hero);
    position: relative;
    overflow: hidden;
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  .hero-label {
    font-size: 0.9rem;
    letter-spacing: 4px;
    color: var(--gold);
    font-weight: 700;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
    animation: fadeIn 0.8s ease-out;
  }

  .hero-text h1 {
    font-size: clamp(3rem, 5vw, 4.5rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
    animation: fadeInUp 0.8s ease-out 0.2s backwards;
  }

  .gradient-text {
    background: var(--gradient-vibrant);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.3rem;
    color: var(--stone);
    margin-bottom: 1.5rem;
    font-style: italic;
    animation: fadeInUp 0.8s ease-out 0.4s backwards;
  }

  .hero-description {
    font-size: 1.1rem;
    line-height: 1.8;
    margin-bottom: 2.5rem;
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
    animation: fadeInUp 0.8s ease-out 0.6s backwards;
  }

  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 3rem;
    animation: fadeInUp 0.8s ease-out 0.8s backwards;
    flex-wrap: wrap;
    justify-content: center;
  }

  .hero-stats {
    display: flex;
    gap: 3rem;
    animation: fadeInUp 0.8s ease-out 1s backwards;
    justify-content: center;
  }

  .stat {
    text-align: center;
  }

  .stat-number {
    font-size: 3rem;
    font-weight: 700;
    background: var(--gradient-vibrant);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
  }

  .stat-label {
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin-top: 0.5rem;
  }

  /* Hero Visual */
  .hero-visual {
    display: flex;
    align-items: center;
    justify-content: center;
    animation: fadeIn 1s ease-out 0.4s backwards;
  }

  .mandala-container {
    position: relative;
    width: 400px;
    height: 400px;
  }

  .mandala-outer,
  .mandala-middle {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    animation: rotate 60s linear infinite;
  }

  .mandala-outer {
    width: 100%;
    height: 100%;
    background: conic-gradient(
      from 0deg,
      var(--gold) 0deg,
      var(--blue) 120deg,
      var(--sage) 240deg,
      var(--gold) 360deg
    );
    opacity: 0.1;
    filter: blur(20px);
  }

  .mandala-middle {
    width: 80%;
    height: 80%;
    background: radial-gradient(circle, var(--blue) 0%, transparent 70%);
    opacity: 0.15;
    animation-duration: 40s;
    animation-direction: reverse;
  }

  .lotus-center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 300px;
    height: 300px;
  }

  .lotus-svg {
    width: 100%;
    height: 100%;
    filter: drop-shadow(0 10px 30px rgba(244, 180, 26, 0.3));
  }

  .petal-group {
    animation: petalPulse 4s ease-in-out infinite;
    transform-origin: center;
  }

  @keyframes rotate {
    from { transform: translate(-50%, -50%) rotate(0deg); }
    to { transform: translate(-50%, -50%) rotate(360deg); }
  }

  @keyframes petalPulse {
    0%, 100% { transform: scale(1); opacity: 0.8; }
    50% { transform: scale(1.05); opacity: 1; }
  }

  /* Section Header */
  .section-header {
    margin-bottom: 4rem;
  }

  .section-label {
    font-size: 0.85rem;
    letter-spacing: 3px;
    color: var(--gold);
    font-weight: 600;
    text-transform: uppercase;
    display: block;
    margin-bottom: 1rem;
  }

  .section-header h2 {
    margin-bottom: 1.5rem;
  }

  .section-header p {
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto;
  }

  /* Challenges Grid */
  .challenges-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* Force single horizontal stack */
    gap: 1.5rem;
    margin-top: 3rem;
    max-width: 1200px;
    margin-left: auto;
    margin-right: auto;
  }

  .challenge-card {
    background: white;
    padding: 2rem; /* Reduced padding */
    border-radius: 20px;
    box-shadow: var(--shadow-md);
    transition: all 0.4s ease;
    animation: fadeInUp 0.8s ease-out backwards;
  }

  .challenge-card:nth-child(1) { animation-delay: 0.1s; }
  .challenge-card:nth-child(2) { animation-delay: 0.2s; }
  .challenge-card:nth-child(3) { animation-delay: 0.3s; }

  .challenge-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-xl);
  }

  .challenge-icon {
    font-size: 4rem;
    margin-bottom: 1.5rem;
  }

  .challenge-card h3 {
    font-size: 1.5rem;
    color: var(--gold);
    margin-bottom: 0.5rem;
  }

  .challenge-card h4 {
    font-size: 1.2rem;
    margin-bottom: 1rem;
    color: var(--blue);
  }

  /* Triad Diagram */
  .triad-diagram {
    max-width: 500px;
    margin: 3rem auto;
  }

  .triad-svg {
    width: 100%;
    height: auto;
  }

  /* Programs Grid */
  .programs-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr); /* Force 2x2 grid */
    gap: 2rem;
    margin-top: 3rem;
    max-width: 1200px; /* Wider grid to reduce side whitespace */
    margin-left: auto;
    margin-right: auto;
  }

  .program-card {
    background: white;
    padding: 2.5rem 2rem;
    border-radius: 20px;
    box-shadow: var(--shadow-md);
    transition: all 0.4s ease;
    position: relative;
    overflow: hidden;
  }

  .program-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 5px;
    background: var(--gradient-vibrant);
    transform: scaleX(0);
    transform-origin: left;
    transition: transform 0.4s ease;
  }

  .program-card:hover::before {
    transform: scaleX(1);
  }

  .program-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-xl);
  }

  .program-icon {
    width: 80px;
    height: 80px;
    background: var(--gradient-hero);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    margin-bottom: 2rem;
    transition: transform 0.4s ease;
  }

  .program-card:hover .program-icon {
    transform: rotate(10deg) scale(1.1);
  }

  .program-card h3 {
    margin-bottom: 1rem;
  }

  .program-card > p {
    margin-bottom: 1.5rem;
  }

  .program-features {
    list-style: none;
    margin-bottom: 2rem;
  }

  .program-features li {
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
    color: var(--text-secondary);
  }

  .program-features li::before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--gold);
    font-weight: bold;
  }

  .program-link {
    color: var(--blue);
    font-weight: 600;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    transition: gap 0.3s ease;
  }

  .program-link:hover {
    gap: 1rem;
  }

  /* Philosophy Quote */
  .philosophy-quote {
    position: relative;
    padding: 3rem 2rem;
    margin: 3rem 0;
  }

  .quote-mark {
    color: var(--gold);
    opacity: 0.2;
    margin: 0 auto 1rem;
  }

  .philosophy-quote p {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    font-style: italic;
    color: var(--text-primary);
    line-height: 1.5;
    margin-bottom: 1.5rem;
  }

  .philosophy-author {
    color: var(--gold);
    font-weight: 600;
    letter-spacing: 2px;
  }

  /* Growth Framework */
  .growth-framework {
    margin-top: 3rem;
    padding: 0;
    background: transparent;
    box-shadow: none;
  }

  .growth-framework h3 {
    margin-bottom: 1rem;
    color: var(--blue);
  }

  .framework-steps {
    display: flex;
    align-items: flex-start; /* Better alignment for text of different lengths */
    justify-content: center;
    gap: 1.5rem;
    margin-top: 3rem;
    flex-wrap: nowrap; /* Force single row */
  }

  .framework-step {
    flex: 1;
    min-width: 140px; /* Allow smaller width to fit in row */
    text-align: center;
  }

  .step-icon {
    width: 80px;
    height: 80px;
    background: var(--gradient-hero);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.5rem;
    margin: 0 auto 1rem;
  }

  .arrow {
    font-size: 2.5rem;
    color: var(--gold);
    font-weight: 300;
    margin-top: 1.5rem; /* Aligns with the center of the 80px icon */
    opacity: 0.6;
  }

  /* Triad Solution */
  .triad-section {
    padding-bottom: 0;
  }

  .triad-visual {
    max-width: 800px;
    margin: 3rem auto 0;
  }

  .triad-img {
    width: 100%;
    height: auto;
    /* Card styling removed as requested */
  }

  .programs-cards-section {
    padding: 4rem 0;
  }

  .programs-cards-section .section-header {
    margin-bottom: 3rem;
  }

  /* Centers */
  .centers-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 3rem;
  }

  .center-card {
    background: white;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: var(--shadow-md);
    transition: all 0.4s ease;
  }

  .center-card:hover {
    transform: translateY(-10px);
    box-shadow: var(--shadow-xl);
  }

  .center-image {
    height: 250px;
    background: var(--gradient-hero);
  }

  .image-placeholder {
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1rem;
  }

  .image-placeholder span {
    font-size: 4rem;
  }

  .image-placeholder p {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--text-primary);
  }

  .center-content {
    padding: 2rem;
  }

  .center-content h3 {
    margin-bottom: 0.5rem;
  }

  .center-content > p {
    color: var(--blue);
    margin-bottom: 1.5rem;
  }

  .center-location {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    color: var(--text-secondary);
  }

  .center-location svg {
    color: var(--gold);
    flex-shrink: 0;
  }

  /* CTA Section */

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
    }

    .hero-description {
      max-width: 100%;
    }

    .hero-buttons {
      justify-content: center;
    }

    .hero-stats {
      justify-content: center;
    }

    .mandala-container {
      width: 300px;
      height: 300px;
    }

    .lotus-center {
      width: 200px;
      height: 200px;
    }

    /* Framework steps stack on mobile now, not tablet */
    /* .framework-steps { flex-direction: column; } removed */

    .arrow {
      transform: rotate(90deg);
      display: none; /* Hide arrows on vertical stack usually, or rotate */
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 6rem 0 3rem;
    }


    .framework-steps {
      flex-direction: column;
      gap: 1rem;
    }

    .programs-grid {
      grid-template-columns: 1fr;
    }

    .challenges-grid {
      grid-template-columns: 1fr;
    }
    
    .arrow {
      transform: rotate(90deg);
      margin: 0.5rem 0;
    }
  }
</style>
